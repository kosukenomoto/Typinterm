# Typinterm
CLI typing trainer sample

# Feature
 - WPM Count ontime.
 - TYPO Count and indicated on type.
 - Read Resource File to type.

## TODO
 - [ ] show WPM Graph
 - [ ] UI Layout redesign

## Build & Test
```
$ cmake -B build -S .
$ cmake --build build
$ cd build && ctest
```
